{#
    This file is part of the CloudStack Client Generator.

    (c) Quentin Pleplé <quentin.pleple@gmail.com>
    (c) Aaron Hurt <ahurt@anbcs.com>
    (c) Nathan Johnson <nathan@nathanjohnson.info>
    (c) Daniel Carbone <daniel.p.carbone@gmail.com>
    (c) Bogdan Gabor <bgabor@ena.com>

    For the full copyright and license information, please view the LICENSE
    file that was distributed with this source code.
#}
{% autoescape false %}
<?php namespace {% if config.namespace != '' %}{{ config.namespace }}\{% endif %}CloudStackResponse;

/*
 * This file was autogenerated as part of the CloudStack PHP Client.
 *
 * Date Generated: {{ config.now.format('Y-m-d') }}
 * API Version: {{ capabilities.capability.cloudstackversion }}
 *
 * (c) Quentin Pleplé {{ '<quentin.pleple@gmail.com>'|raw }}
 * (c) Aaron Hurt {{ '<ahurt@anbcs.com>'|raw }}
 * (c) Nathan Johnson {{ '<nathan@nathanjohnson.org>'|raw }}
 * (c) Daniel Carbone {{ '<daniel.p.carbone@gmail.com>'|raw }}
 * (c) Bogdan Gabor {{ '<bgabor@ena.com>'|raw }}
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

/**
 * Class AsyncJobStartResponse
 * @package \{% if config.namespace != '' %}{{ config.namespace }}\{% endif %}CloudStackResponse
 * @SWG\Definition(definition="AsyncJobStartResponse")
 */
class AsyncJobStartResponse {

    /**
     * @SWG\Property(type="string")
     * @var string
     */
    public $id;

    /**
     * @SWG\Property(type="string", description="Async Job ID")
     * @var string
     */
    public $jobid;

    /**
     * Holds the FQ ClassName of the object to construct if the async job is successful.
     * @var string
     */
    private $responseClassName;

    /**
     * AsyncJobStartResponse Constructor
     * @param array $data
     */
    public function __construct(array $data, $responseClassName) {
        if (isset($data['id']) && is_string($data['id'])) {
            $this->id = trim($data['id']);
        }
        if (isset($data['jobid']) && is_string($data['jobid'])) {
            $this->jobid = trim($data['jobid']);
        }

        $this->responseClassName = $responseClassName;
    }

    /**
     * @return string
     */
    public function getResponseClassName() {
        return $this->responseClassName;
    }
}
{% endautoescape %}